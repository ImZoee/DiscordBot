# Discord Join-and-Play Bot

**Setup rapid**

- Editeaza fisierul `.env` (vezi `.env.example`) cu `DISCORD_TOKEN`.
- Alege un fisier MP3 si adauga-l in path-ul `audio/clip.mp3` sau setează `AUDIO_PATH` în `.env`.

```bash
npm install
npm start
```

Permisiuni bot: `Connect` și `Speak`.
